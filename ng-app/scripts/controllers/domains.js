angular.module('knownlyApp.controllers', [])

	// Domains Controller
	.controller('DomainsCtrl', ['$scope', function($scope) {    
	 
	  $scope.init = function() {
	    // initialise
	  };

	  $scope.init();

	}]);
